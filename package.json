{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vitepress build docs && node scripts/build-merge.js",
    "preview": "npm run build && wrangler dev",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "vite": "^8.0.12",
    "vitepress": "^1.6.4",
    "wrangler": "^4.98.0"
  }
}
